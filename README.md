# Looklet

* Install: `npm i`
* Run: `npm start`
* Build: `npm run build`

To run the build locally, you need to run a web server. An easy way to achieve this is with:

* `npm i -g serve`
* `serve -s build`
